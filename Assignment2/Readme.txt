Prepare step:
1)start the hdfs system
2)move the data file to the hdfs 
   bin/hdfs dfs -put /task1-input1.txt /
   bin/hdfs dfs -put /task1-input2.txt /
   bin/hdfs dfs -put /stopwords.txt /
   bin/hdfs dfs -put /QA_data.csv /
3) You can run the code in your IDE, the source code in the folder source code
4) You can check the result in the folder result file
5) spark version:2.4.0  scala version:2.11.11  you can run it in eclipse.
